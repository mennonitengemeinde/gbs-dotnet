@page "/admin"
@attribute [Authorize]
@inject NavigationManager NavigationManager

@code {
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo("/admin/streams", replace: true);
    }
}